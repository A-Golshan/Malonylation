import '../Styles/Loader.css';

const Loader = ({ size }) => {
    return (
        <div className="loader">
            <span></span>
            <span></span>
            <span></span>
        </div>
    );
}

export default Loader;